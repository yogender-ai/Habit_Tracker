{
  "name": "dayforge-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "api": "uvicorn app:app --host 127.0.0.1 --port 8000",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "firebase": "^12.13.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
